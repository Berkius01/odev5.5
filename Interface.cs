using System;
using System.Collections.Generic;
using System.Text;

namespace ödev5._5
{
    interface Interface
    {
        void Add(Register reg);
        void Update(Register reg);
        void Delete(Register reg);
    }
}
